You'll need:

* Vagrant
* Ansible
* Virtualbox

Commands:

* `vagrant up` - from zero to fully configured
* `vagrant provision` - run Ansible on an already-running VM
* `vagrant destroy` - from fully configured back to zero
* `vagrant ssh` - open a terminal in the VMh
